services:
  - type: web
    name: CampusCore
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn CampusCore.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
